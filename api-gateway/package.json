{
  "name": "ag-mcp-api-gateway",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma db push --skip-generate && node index.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cloudinary": "^1.41.0",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
