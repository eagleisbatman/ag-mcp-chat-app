appId: org.digitalgreen.agmcp
name: "A2UI - Picker Search and Filter (WIP)"
tags:
  - a2ui
  - wip
properties:
  testCaseId: "TC-A2UI-003"
  priority: "Medium"
---
# [WIP] A2UI picker search requires a picker widget to be rendered first,
# which depends on a non-deterministic AI response.
# Excluded from default regression run via `wip` tag.

- startRecording: "a2ui-picker-search"

- runScript: ../../scripts/page-objects.js

- runFlow: ../../subflows/ensure-logged-in.yaml

- runFlow: ../../subflows/navigate-to-chat.yaml

- takeScreenshot: "a2ui-picker-search-01-placeholder"

- stopRecording
