export const ELEVATION = {
  sm: {
    // Intentionally flat (no shadows/elevation)
  },
  md: {
    // Intentionally flat (no shadows/elevation)
  },
  lg: {
    // Intentionally flat (no shadows/elevation)
  },
};
